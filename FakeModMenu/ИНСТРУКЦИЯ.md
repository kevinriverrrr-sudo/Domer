# 📱 Инструкция по сборке и подписанию APK

## 🚀 Быстрый старт

### Шаг 1: Создание ключа для подписи

```bash
cd /workspace/FakeModMenu
./create_keystore.sh
```

При создании ключа вам нужно будет ввести:
- **Пароль keystore** (запомните его!)
- **Пароль ключа** (запомните его!)
- Информацию об организации (можно просто нажимать Enter)

### Шаг 2: Создание файла конфигурации

Создайте файл `keystore.properties` в корне проекта:

```
storePassword=ваш_пароль_keystore
keyPassword=ваш_пароль_ключа
keyAlias=modmenu
storeFile=keystore.jks
```

### Шаг 3: Сборка подписанного APK

```bash
./gradlew assembleRelease
```

Готовый подписанный APK будет находиться в:
```
app/build/outputs/apk/release/app-release.apk
```

## 📦 Установка на телефон

### Через USB:
```bash
adb install app/build/outputs/apk/release/app-release.apk
```

### Через файл:
1. Скопируйте файл `app-release.apk` на телефон
2. Откройте файл на телефоне
3. Разрешите установку из неизвестных источников, если потребуется

## 🎮 Как пользоваться приложением

1. **Запустите** приложение "Mod Menu"
2. **Разрешите** отображение поверх других приложений (появится запрос)
3. **Нажмите** кнопку "Запустить"
4. **Мод меню появится** в виде плавающего окна
5. **Перетаскивайте** окно за верхнюю часть (заголовок)
6. **Скрывайте/показывайте** окно кнопкой "×"
7. **Переключайтесь** между вкладками:
   - 👁️ Visuals - визуальные эффекты
   - 🎯 AimBot - автонаведение
   - ⚙️ Misc - разные функции
   - 👤 Player - настройки игрока
   - 🔧 Settings - настройки меню

## ⚠️ ВАЖНОЕ НАПОМИНАНИЕ

Это **демонстрационное приложение**!
- ❌ Функции НЕ работают
- ❌ Не используйте в реальных играх
- ✅ Создано только для обучения
- ✅ Показывает только UI

## 🔧 Возможные проблемы

### Проблема: "keytool не найден"
**Решение**: Установите JDK
```bash
sudo apt install openjdk-11-jdk
```

### Проблема: "Android SDK не найден"
**Решение**: Укажите путь к SDK в файле `local.properties`:
```
sdk.dir=/путь/к/вашему/Android/Sdk
```

### Проблема: "Не удается установить APK"
**Решение**: 
1. Включите "Установку из неизвестных источников" в настройках телефона
2. Убедитесь, что старая версия удалена
3. Проверьте, что APK подписан

## 📊 Характеристики приложения

- **Размер**: ~2-5 МБ
- **Минимальная версия Android**: 5.0 (API 21)
- **Разрешения**: 
  - Отображение поверх других приложений
  - Фоновый сервис

## 🛠️ Для разработчиков

### Debug сборка (без подписи):
```bash
./gradlew assembleDebug
```

### Очистка проекта:
```bash
./gradlew clean
```

### Просмотр логов:
```bash
adb logcat | grep FakeModMenu
```

### Удаление с устройства:
```bash
adb uninstall com.example.fakemodmenu
```

---

💡 **Совет**: Сохраните файлы `keystore.jks` и `keystore.properties` в безопасном месте! Они понадобятся для обновления приложения в будущем.
