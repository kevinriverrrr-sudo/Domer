# 📱 Руководство по установке MoonCode

## Быстрая установка

### Шаг 1: Загрузка
Найдите файл `MoonCode-v1.0-signed.apk` в корне проекта (размер: ~4.7 MB)

### Шаг 2: Включение установки из неизвестных источников
1. Откройте **Настройки** на вашем Android устройстве
2. Перейдите в **Безопасность** или **Приложения**
3. Найдите опцию **Установка из неизвестных источников** или **Разрешить установку приложений из этого источника**
4. Включите эту опцию для файлового менеджера или браузера

### Шаг 3: Установка APK
1. Откройте файл `MoonCode-v1.0-signed.apk`
2. Нажмите **Установить**
3. Дождитесь завершения установки
4. Нажмите **Открыть**

### Шаг 4: Предоставление разрешений
При первом запуске приложению потребуются следующие разрешения:
- **Доступ к файлам и медиа** - для сохранения и открытия Lua файлов
- Предоставьте необходимые разрешения для корректной работы

### Шаг 5: Первый запуск
1. После запуска вы увидите редактор кода с шаблоном Lua скрипта
2. Используйте кнопки внизу справа для:
   - ➕ Создание нового файла
   - 📂 Открытие существующего файла
   - 💾 Сохранение файла
   - 📤 Компиляция в Luac

## 🔧 Настройка

### Изменение темы
1. Откройте меню (⋮) в правом верхнем углу
2. Выберите **Настройки**
3. Переключите **Темная тема** (по умолчанию включена)

### Настройка размера шрифта
1. Откройте **Настройки**
2. Нажмите **Размер шрифта**
3. Выберите желаемый размер (Маленький/Средний/Большой/Очень большой)

## 📂 Где хранятся файлы

Все созданные и сохраненные файлы находятся в:
```
/storage/emulated/0/MoonCode/
```
или
```
/sdcard/MoonCode/
```

Вы можете получить доступ к этой папке через любой файловый менеджер.

## 🚀 Быстрый старт

### Создание первого скрипта

1. **Откройте MoonCode**
2. **Введите свой код** (или используйте шаблон)
3. **Сохраните файл**: Нажмите 💾, введите имя (например: `test.lua`)
4. **Скомпилируйте** (опционально): Нажмите 📤 для создания .luac файла

### Пример простого скрипта

```lua
-- Простой скрипт приветствия
script_name('HelloWorld')
script_author('@MarkusGarantor')
script_version('1.0')

function main()
    if not isSampLoaded() or not isSampfuncsLoaded() then return end
    while not isSampAvailable() do wait(100) end

    sampAddChatMessage('Привет из MoonCode!', 0x00FF00)
    
    while true do
        wait(0)
        -- Ваш код здесь
    end
end
```

## ❓ Часто задаваемые вопросы

### Q: Приложение не открывает файлы
**A**: Убедитесь, что вы предоставили разрешения на доступ к файлам в настройках Android.

### Q: Где находится скомпилированный .luac файл?
**A**: В той же директории, что и исходный .lua файл, с расширением .luac

### Q: Можно ли использовать приложение без Moonloader?
**A**: Да! MoonCode - это полноценный редактор кода. Вы можете писать и сохранять скрипты без Moonloader. Однако для запуска скриптов в GTA SA вам потребуется Moonloader.

### Q: Как перенести файлы с компьютера?
**A**: Используйте USB кабель или облачное хранилище (Google Drive, Dropbox и т.д.), чтобы скопировать .lua файлы в папку `/sdcard/MoonCode/`

### Q: Приложение крашится при запуске
**A**: 
- Убедитесь, что у вас Android 5.0 или выше
- Переустановите приложение
- Очистите кэш и данные приложения

### Q: Как изменить язык приложения?
**A**: В текущей версии поддерживается только русский язык. Английский язык планируется в следующих версиях.

## 🔒 Безопасность

APK подписан сертификатом разработчика:
- **CN**: MarkusGarantor
- **SHA-256**: `7905cbd4d6e09caf65b9fab0652be784b653f2e2cd69ac29e3fec381e17177ef`

Вы можете проверить подпись с помощью команды:
```bash
apksigner verify --print-certs MoonCode-v1.0-signed.apk
```

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте раздел FAQ выше
2. Убедитесь, что у вас последняя версия приложения
3. Свяжитесь с разработчиком: **@MarkusGarantor**

---

**Приятного кодинга! 🌙✨**
