# 📱 Руководство по развертыванию ХроноКолода

## Варианты запуска игры

### 🌐 Вариант 1: Веб-версия (работает немедленно!)

Игра готова к запуску в браузере:
```bash
# Просто откройте файл в браузере
open www/index.html
# или
firefox www/index.html
# или
google-chrome www/index.html
```

### 📦 Вариант 2: Создание APK через онлайн-сервисы (РЕКОМЕНДУЕТСЯ)

Самый простой способ получить APK без установки Android SDK:

#### AppsGeyser (бесплатно)
1. Перейдите на https://appsgeyser.com
2. Выберите "Website"
3. Загрузите файлы из папки `www/` или укажите URL
4. Настройте иконку и название
5. Скачайте готовый APK

#### Hermit Lite Apps
1. Установите Hermit из Google Play
2. Откройте локальный файл index.html
3. Создайте lite app на домашнем экране

#### WebIntoApp
1. Перейдите на https://app.webintoapp.com
2. Загрузите ZIP с игрой
3. Настройте параметры
4. Скачайте APK

### 🔧 Вариант 3: Сборка APK через Android Studio

1. Установите [Android Studio](https://developer.android.com/studio)
2. Откройте проект из папки `android/`
3. Подождите синхронизацию Gradle
4. Build → Build Bundle(s) / APK(s) → Build APK(s)
5. APK будет в `android/app/build/outputs/apk/debug/`

### ⚙️ Вариант 4: Командная строка (для разработчиков)

#### Установка необходимых инструментов:

```bash
# Установите Android SDK
# Ubuntu/Debian:
sudo apt-get install android-sdk

# macOS:
brew install --cask android-sdk

# Или скачайте с официального сайта
# https://developer.android.com/studio#command-tools
```

#### Установка Gradle:

```bash
# Ubuntu/Debian:
sudo apt-get install gradle

# macOS:
brew install gradle

# Или скачайте с https://gradle.org/install/
```

#### Сборка APK:

```bash
cd android
./gradlew assembleDebug

# Для release версии:
./gradlew assembleRelease
```

APK будет создан в:
- Debug: `android/app/build/outputs/apk/debug/app-debug.apk`
- Release: `android/app/build/outputs/apk/release/app-release-unsigned.apk`

### 🔐 Вариант 5: Подпись APK

Для публикации в Google Play нужен подписанный APK:

```bash
# 1. Создайте keystore
keytool -genkey -v -keystore chrono-deck.keystore \
  -alias chrono-deck-key -keyalg RSA -keysize 2048 -validity 10000

# 2. Соберите release APK
cd android
./gradlew assembleRelease

# 3. Подпишите APK
jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA-256 \
  -keystore ../chrono-deck.keystore \
  app/build/outputs/apk/release/app-release-unsigned.apk \
  chrono-deck-key

# 4. Выровняйте APK (требуется Android SDK)
zipalign -v 4 \
  app/build/outputs/apk/release/app-release-unsigned.apk \
  ChronoDeck-v1.0.0-signed.apk
```

Или используйте скрипт:
```bash
./sign-apk.sh
```

## 🚀 Быстрый старт для пользователей

Если вы просто хотите поиграть:

1. **На компьютере**: Откройте `www/index.html` в браузере
2. **На Android**: 
   - Установите APK (если есть)
   - Или откройте `www/index.html` в Chrome и добавьте на главный экран

## 📦 Готовые пакеты

После запуска `build-simple-apk.sh` создаются:
- `ChronoDeck-WebGame-v1.0.0.zip` - веб-версия для распространения
- `ChronoDeck-WebGame-v1.0.0.tar.gz` - архив для Linux/Mac
- `build/` - папка с готовыми файлами

## 🔍 Требования к системе

### Для веб-версии:
- Любой современный браузер (Chrome, Firefox, Safari, Edge)
- JavaScript должен быть включен
- LocalStorage для сохранения прогресса

### Для Android APK:
- Android 5.1 (API 22) или выше
- ~10 МБ свободного места
- Разрешение на установку из неизвестных источников (для APK не из Play Store)

## 🐛 Решение проблем

### Ошибка: "Gradle не найден"
```bash
# Установите Gradle
wget https://services.gradle.org/distributions/gradle-8.0.2-bin.zip
unzip gradle-8.0.2-bin.zip
export PATH=$PATH:$PWD/gradle-8.0.2/bin
```

### Ошибка: "Android SDK не найден"
Установите Android Studio или только SDK:
```bash
# Скачайте command line tools
wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
unzip commandlinetools-linux-9477386_latest.zip
export ANDROID_HOME=$PWD/cmdline-tools
```

### Игра не сохраняет прогресс
- Убедитесь, что LocalStorage включен в браузере
- Для Android: дайте разрешения на хранение данных

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Убедитесь, что JavaScript включен
3. Попробуйте другой браузер
4. Для APK: проверьте логи Android (adb logcat)

## 🎉 Успешная установка!

После успешной установки вы увидите:
- Главное меню с кнопками "Новая игра", "Продолжить", "Коллекция карт"
- Ваш уровень и ХроноКристаллы
- Возможность начать путешествие сквозь эпохи!

Приятной игры! ⏳
