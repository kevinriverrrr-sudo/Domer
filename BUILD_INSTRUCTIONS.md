# Инструкция по сборке APK файла

## 🚀 Быстрый старт

### Способ 1: Через Docker (Рекомендуется - не требует установки Android SDK)

```bash
chmod +x build-apk-docker.sh
./build-apk-docker.sh
```

Этот способ использует Docker контейнер с предустановленным Android SDK и не требует локальной установки.

### Способ 2: Локальная сборка

## Требования

Для сборки APK файла вам понадобится:

1. **Node.js** (версия 14 или выше)
   - Скачать: https://nodejs.org/
   - Проверить установку: `node --version`

2. **Java JDK** (версия 11 или выше)
   - Скачать: https://www.oracle.com/java/technologies/downloads/
   - Проверить установку: `java -version`

3. **Android SDK** (через Android Studio)
   - Скачать Android Studio: https://developer.android.com/studio
   - Установить Android SDK через Android Studio
   - Установить переменные окружения:
     - `ANDROID_HOME` - путь к Android SDK
     - Добавить в PATH: `%ANDROID_HOME%\platform-tools` и `%ANDROID_HOME%\tools`

## Быстрая сборка

### Linux/macOS:
```bash
chmod +x build-apk.sh
./build-apk.sh
```

### Windows:
```cmd
build-apk.bat
```

## Ручная сборка

### Шаг 1: Установка зависимостей
```bash
npm install
```

### Шаг 2: Инициализация Capacitor (если еще не сделано)
```bash
npx cap add android
```

### Шаг 3: Синхронизация файлов
```bash
npx cap sync android
```

### Шаг 4: Открытие проекта в Android Studio (опционально)
```bash
npx cap open android
```

### Шаг 5: Сборка APK
```bash
cd android
./gradlew assembleRelease  # Linux/macOS
gradlew.bat assembleRelease  # Windows
```

Готовый APK будет находиться в:
`android/app/build/outputs/apk/release/app-release.apk`

## Подпись APK (для публикации в Google Play)

Для подписи APK создайте файл `keystore`:

```bash
keytool -genkey -v -keystore lua-generator-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias lua-generator
```

Затем обновите `capacitor.config.json` с путями к keystore.

## Альтернативные способы сборки

### Способ 3: Использование онлайн-сервисов

Если у вас нет Docker и Android SDK, вы можете использовать онлайн-сервисы:

1. **PhoneGap Build** (https://build.phonegap.com/)
   - Загрузите файлы из папки `dist/`
   - Создайте `config.xml` для PhoneGap
   - Соберите APK онлайн

2. **Capacitor Cloud Build** (платный сервис)
   - Используйте Capacitor Cloud для автоматической сборки

3. **GitHub Actions** (бесплатно для публичных репозиториев)
   - Используйте готовый workflow для сборки APK

### Способ 4: Использование готового APK

Если сборка не удается, можно использовать веб-версию приложения:
1. Откройте `index.html` в мобильном браузере
2. Добавьте на главный экран через меню браузера
3. Приложение будет работать как PWA

## Использование готового APK

1. Скопируйте APK файл на Android устройство
2. Разрешите установку из неизвестных источников в настройках безопасности
3. Установите APK файл
4. Запустите приложение

## Устранение проблем

### Ошибка: "ANDROID_HOME не установлен"
- Установите Android Studio
- Установите переменную окружения ANDROID_HOME

### Ошибка: "Gradle не найден"
- Убедитесь, что Android SDK установлен
- Запустите `npx cap sync android` заново

### Ошибка при сборке
- Откройте проект в Android Studio: `npx cap open android`
- Проверьте ошибки в Android Studio
- Убедитесь, что все зависимости установлены

## Дополнительная информация

- Capacitor документация: https://capacitorjs.com/docs
- Android разработка: https://developer.android.com/
