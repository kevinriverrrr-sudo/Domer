{
  "name": "lua-script-generator",
  "version": "1.0.0",
  "description": "Генератор Lua скриптов для GTA San Andreas Multiplayer",
  "main": "index.html",
  "scripts": {
    "build": "npm run sync",
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "build:android": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "keywords": [
    "lua",
    "gta",
    "samp",
    "script-generator"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@capacitor/cli": "^5.5.0"
  },
  "dependencies": {
    "@capacitor/core": "^5.5.0",
    "@capacitor/android": "^5.5.0",
    "@capacitor/app": "^5.0.6",
    "@capacitor/filesystem": "^5.1.4"
  }
}
