# 📦 Сборка APK для FunPay Mobile

## 🚀 Методы сборки APK

У вас есть 2 способа собрать APK:

### ✅ Метод 1: EAS Build (Рекомендуется) - Автоматическая подпись

Это самый простой способ - Expo автоматически создаст и подпишет APK!

#### Шаг 1: Установите EAS CLI

```bash
npm install -g eas-cli
```

#### Шаг 2: Войдите в Expo (или создайте аккаунт)

```bash
eas login
```

Если у вас нет аккаунта:
```bash
eas register
```

#### Шаг 3: Настройте проект

```bash
eas build:configure
```

#### Шаг 4: Соберите APK

**Для тестирования (быстрее):**
```bash
npm run build:preview
```

**Для продакшена (с автоматической подписью):**
```bash
npm run build:production
```

**Альтернативная команда:**
```bash
eas build -p android --profile apk
```

#### Что произойдет:

1. ✅ EAS Build автоматически создаст keystore для подписи
2. ✅ Соберет APK в облаке
3. ✅ Подпишет APK автоматически
4. ✅ Даст ссылку на скачивание готового APK

⏱️ Время сборки: 10-20 минут

📱 Результат: Готовый подписанный APK файл!

---

### 🔧 Метод 2: Локальная сборка с Expo (без облака)

Если не хотите использовать облако:

#### Шаг 1: Установите Android Studio и настройте

1. Скачайте [Android Studio](https://developer.android.com/studio)
2. Установите Android SDK
3. Настройте переменные окружения:
   ```bash
   export ANDROID_HOME=$HOME/Android/Sdk
   export PATH=$PATH:$ANDROID_HOME/emulator
   export PATH=$PATH:$ANDROID_HOME/tools
   export PATH=$PATH:$ANDROID_HOME/tools/bin
   export PATH=$PATH:$ANDROID_HOME/platform-tools
   ```

#### Шаг 2: Соберите APK

```bash
# Установите зависимости
npm install

# Создайте нативные папки android/ios
npx expo prebuild

# Соберите APK
cd android
./gradlew assembleRelease
```

APK будет в: `android/app/build/outputs/apk/release/app-release.apk`

---

## 🔑 Подпись APK (для локальной сборки)

Если вы собираете локально, нужно подписать APK:

### Шаг 1: Создайте keystore

```bash
keytool -genkeypair -v -storetype PKCS12 -keystore funpay-release-key.keystore -alias funpay-key-alias -keyalg RSA -keysize 2048 -validity 10000
```

Введите пароль и информацию о себе.

### Шаг 2: Настройте gradle

Создайте файл `android/gradle.properties`:

```properties
FUNPAY_UPLOAD_STORE_FILE=funpay-release-key.keystore
FUNPAY_UPLOAD_KEY_ALIAS=funpay-key-alias
FUNPAY_UPLOAD_STORE_PASSWORD=ваш_пароль
FUNPAY_UPLOAD_KEY_PASSWORD=ваш_пароль
```

### Шаг 3: Соберите подписанный APK

```bash
cd android
./gradlew assembleRelease
```

---

## 📥 Скачивание готового APK

После успешной сборки через EAS Build:

1. Вы получите ссылку в терминале
2. Или проверьте на: https://expo.dev/accounts/[ваш-аккаунт]/projects/funpay-mobile/builds
3. Скачайте APK и установите на Android устройство

---

## 🎯 Быстрая команда для сборки

**Самый простой способ (рекомендуется):**

```bash
# Установите EAS CLI (один раз)
npm install -g eas-cli

# Войдите в Expo
eas login

# Соберите APK с автоматической подписью
npm run build:production
```

Готово! ✅ Expo автоматически подпишет ваш APK!

---

## 📱 Установка APK на телефон

### Способ 1: Прямое скачивание

1. Откройте ссылку из EAS Build на телефоне
2. Скачайте APK
3. Разрешите установку из неизвестных источников
4. Установите APK

### Способ 2: Через ADB

```bash
# Подключите телефон по USB
# Включите отладку по USB на телефоне

# Установите APK
adb install app-release.apk
```

### Способ 3: Через компьютер

1. Скачайте APK на компьютер
2. Скопируйте на телефон через USB
3. Откройте файловый менеджер на телефоне
4. Найдите APK и установите

---

## 🔒 Безопасность keystore

**ВАЖНО:**

- ❌ НЕ загружайте keystore в git
- ✅ Сохраните keystore в безопасном месте
- ✅ Запомните пароли
- ✅ Сделайте резервную копию

Если потеряете keystore, не сможете обновить приложение в Google Play!

---

## 🎉 Готовые команды

**Для быстрой сборки APK с подписью через EAS:**

```bash
npm install -g eas-cli
eas login
npm run build:production
```

**Для локальной сборки:**

```bash
npm install
npx expo prebuild
cd android && ./gradlew assembleRelease
```

---

## 🆘 Возможные проблемы

### Ошибка: "eas: command not found"

```bash
npm install -g eas-cli
```

### Ошибка: "Not logged in"

```bash
eas login
```

### Ошибка при локальной сборке

Убедитесь, что:
- Установлен Android Studio
- Настроен ANDROID_HOME
- Установлен JDK 11+

---

## 📊 Сравнение методов

| Метод | Сложность | Время | Подпись | Рекомендация |
|-------|-----------|-------|---------|--------------|
| EAS Build | ⭐ Легко | 10-20 мин | ✅ Авто | ✅ Рекомендуется |
| Локальная | ⭐⭐⭐ Сложно | 5 мин | ⚠️ Вручную | Для опытных |

---

## 🎯 Итог

**Рекомендуемый способ:**

```bash
npm install -g eas-cli
eas login
npm run build:production
```

Expo автоматически:
- ✅ Создаст keystore
- ✅ Соберет APK
- ✅ Подпишет APK
- ✅ Даст ссылку на скачивание

**Время: 10-20 минут**  
**Результат: Готовый подписанный APK!** 🎉
