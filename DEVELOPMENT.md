# Руководство по установке и тестированию

## Установка расширения

1. **Сборка расширения:**
   ```bash
   npm install
   npm run build
   ```

2. **Установка в Chrome:**
   - Откройте Chrome и перейдите по адресу `chrome://extensions/`
   - Включите "Режим разработчика" (右上角 toggle)
   - Нажмите "Загрузить распакованное расширение"
   - Выберите папку `dist` в проекте

3. **Проверка установки:**
   - Расширение должно появиться в списке
   - Иконка 📝 должна быть видна

## Тестирование

### На Funpay.com
1. Перейдите на любую страницу `funpay.com`
2. В правом нижнем углу должна появиться кнопка 📝 "Пакетный покупатель"

### На локальной тестовой странице
1. Откройте файл `test.html` в браузере
2. Кнопка должна появиться на странице

### Проверка функционала

1. **Появление кнопки:**
   - ✅ Кнопка видна в правом нижнем углу
   - ✅ Иконка 📝 и текст "Пакетный покупатель"

2. **Перетаскивание:**
   - ✅ Кнопку можно перетащить мышью
   - ✅ Позиция сохраняется после перезагрузки страницы

3. **Боковая панель:**
   - ✅ По клику на кнопку открывается панель справа
   - ✅ Панель имеет заголовок "Пакетный покупатель v1.0.0"
   - ✅ Есть вкладки "Шаблоны", "Мониторинг", "История"
   - ✅ Кнопка закрытия работает

4. **Сохранение состояния:**
   - ✅ Состояние панели (открыта/закрыта) сохраняется
   - ✅ При перезагрузке страницы состояние восстанавливается

5. **Сворачивание кнопки:**
   - ✅ Кнопку можно свернуть (кнопка ×)
   - ✅ Появляется метка "📝 Показать"
   - ✅ По клику на метку кнопка восстанавливается

6. **Адаптивность:**
   - ✅ Стили работают в светлой теме
   - ✅ Стили адаптируются под тёмную тему
   - ✅ Shadow DOM изолирует стили от сайта

## Разработка

### Режим разработки
```bash
npm run dev
```

### Структура файлов
- `src/content.ts` - основной контент-скрипт
- `src/panel.ts` - боковая панель (Shadow DOM)
- `src/draggable.ts` - логика перетаскивания кнопки
- `src/storage.ts` - работа с chrome.storage.local
- `manifest.json` - манифест расширения MV3

### Сборка
- TypeScript компилируется в ES2020
- Vite собирает модули
- Результат в папке `dist/`

## Отладка

1. **Консоль разработчика:**
   - F12 → Console для просмотра логов
   - Проверьте наличие ошибок

2. **Chrome DevTools для расширений:**
   - `chrome://extensions/` → "Просмотр фоновых страниц"
   - Проверьте работу content script

3. **Хранилище:**
   - `chrome://extensions/` → "Просмотр фоновых страниц" → Application → Storage
   - Проверьте сохраненные данные в chrome.storage.local

## Известные проблемы

- SVG иконки могут не отображаться в некоторых версиях Chrome
- При быстрой навигации по сайту возможны дублирующие инъекции (защищено проверкой)

## Совместимость

- ✅ Chrome 88+
- ✅ Edge 88+
- ✅ Manifest V3
- ✅ TypeScript 5.2+