# 📱 Быстрая инструкция по сборке APK

## ✅ Что уже готово

- ✅ Android проект создан (`android/` директория)
- ✅ Capacitor настроен и синхронизирован
- ✅ Все файлы скопированы в `dist/`
- ✅ Gradle wrapper готов к использованию

## 🚀 Способы сборки APK

### 1. Через Docker (Самый простой способ)

```bash
./build-apk-docker.sh
```

**Требования:** Только Docker

### 2. Локальная сборка

```bash
# Linux/macOS
./build-apk.sh

# Windows
build-apk.bat
```

**Требования:** 
- Node.js
- Java JDK 11+
- Android SDK (через Android Studio)

### 3. Через Android Studio

1. Откройте Android Studio
2. File → Open → выберите папку `android/`
3. Build → Build Bundle(s) / APK(s) → Build APK(s)
4. Готовый APK будет в `android/app/build/outputs/apk/debug/` или `release/`

### 4. Через GitHub Actions (Автоматическая сборка)

Если проект на GitHub:
1. Файл `.github/workflows/build-apk.yml` уже создан
2. Просто запустите workflow в Actions
3. APK будет доступен в Artifacts

## 📦 Где найти готовый APK

После сборки APK будет находиться в:
- `android/app/build/outputs/apk/release/app-release.apk` (release версия)
- `android/app/build/outputs/apk/debug/app-debug.apk` (debug версия)
- `lua-script-generator.apk` (если использовались скрипты сборки)

## ⚠️ Важные замечания

1. **Для release сборки** нужна подпись APK (keystore)
2. **Debug APK** можно установить без подписи
3. **Android SDK** можно установить через Android Studio
4. **Java версия** должна быть 11 или выше (но не слишком новой для Gradle)

## 🔧 Устранение проблем

### Ошибка: "ANDROID_HOME не установлен"
```bash
export ANDROID_HOME=/path/to/android/sdk
export PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools
```

### Ошибка: "Gradle не найден"
```bash
cd android
chmod +x gradlew
./gradlew --version
```

### Ошибка версии Java
Установите Java 11:
```bash
sudo apt install openjdk-11-jdk  # Ubuntu/Debian
```

## 📱 Установка APK на устройство

1. Скопируйте APK на Android устройство
2. Включите "Установка из неизвестных источников" в настройках
3. Откройте APK файл и установите
4. Запустите приложение "Lua Script Generator"

## 🌐 Альтернатива: PWA версия

Если сборка APK не удается, используйте веб-версию:
1. Откройте `index.html` в мобильном браузере
2. Добавьте на главный экран через меню браузера
3. Работает как полноценное приложение (PWA)
