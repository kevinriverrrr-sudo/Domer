# 🔧 Инструкция по настройке и запуску Card Checker Bot

## ✅ Что уже сделано

✅ Все файлы созданы  
✅ Зависимости установлены  
✅ Бот протестирован и работает  
✅ Telegram Bot Token настроен

## ⚠️ Что нужно настроить

### 1. PayPal API Credentials

Бот использует PayPal API для проверки карт. Вам нужно получить credentials:

**Шаги:**

1. Перейдите на https://developer.paypal.com/
2. Войдите или зарегистрируйтесь
3. Перейдите в "Dashboard" → "My Apps & Credentials"
4. Создайте новое приложение (Create App)
5. Скопируйте **Client ID** и **Secret**

**Для тестирования (Sandbox):**
- Используйте Sandbox credentials
- URL: `https://api-m.sandbox.paypal.com`
- Можете использовать тестовые карты PayPal

**Для продакшена:**
- Используйте Live credentials (требуется верификация бизнес-аккаунта)
- URL: `https://api-m.paypal.com`

### 2. Обновите config.py

Откройте файл `config.py` и замените:

```python
PAYPAL_CLIENT_ID = "your_paypal_client_id_here"  # <-- Вставьте ваш Client ID
PAYPAL_SECRET = "your_paypal_secret_here"        # <-- Вставьте ваш Secret
```

## 🚀 Запуск бота

### Вариант 1: Простой запуск (для тестирования)

```bash
cd /workspace
python3 bot.py
```

Бот запустится в текущем терминале. Для остановки нажмите `Ctrl+C`.

### Вариант 2: Запуск в фоновом режиме

```bash
cd /workspace
nohup python3 bot.py > bot.log 2>&1 &
```

Бот будет работать в фоне. Логи будут записываться в `bot.log`.

**Посмотреть логи:**
```bash
tail -f /workspace/bot.log
```

**Остановить бота:**
```bash
pkill -f "python3 bot.py"
```

### Вариант 3: Использование screen (рекомендуется)

```bash
# Установить screen (если нет)
sudo apt-get install screen

# Создать новую сессию
screen -S cardbot

# Запустить бота
cd /workspace
python3 bot.py

# Отключиться от сессии (не закрывая бота): Ctrl+A затем D

# Вернуться к боту позже
screen -r cardbot
```

### Вариант 4: Автозапуск через systemd (для продакшена)

Создайте файл `/etc/systemd/system/cardbot.service`:

```ini
[Unit]
Description=Card Checker Telegram Bot
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/workspace
ExecStart=/usr/bin/python3 /workspace/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable cardbot
sudo systemctl start cardbot

# Проверить статус
sudo systemctl status cardbot

# Посмотреть логи
sudo journalctl -u cardbot -f
```

## 📱 Использование бота

1. Найдите вашего бота в Telegram (используя токен вы можете получить username)
2. Отправьте `/start`
3. Следуйте инструкциям бота

### Тестовые карты PayPal Sandbox

Для тестирования в Sandbox можете использовать эти карты:

**Успешные карты:**
- VISA: `4032039893074081`
- MasterCard: `5425233430109903`
- AMEX: `371449635398431`

**Параметры для всех:**
- Месяц: любой (01-12)
- Год: любой будущий (например, 2025-2030)
- CVV: любой 3-4 цифры

**Формат для бота:**
```
4032039893074081|12|2025|123
```

## 🔍 Проверка работы

После запуска бота вы увидите:
```
🚀 Запуск Card Checker Bot...
📱 Bot Token: 7560458678:AAHbtiK7z...
✅ Бот запущен и готов к работе!
⏳ Ожидание сообщений...
```

Это означает что бот успешно подключился к Telegram и ждет сообщений.

## 🐛 Устранение проблем

### Ошибка "Не удалось получить токен доступа к PayPal API"

**Причина:** Неправильные PayPal credentials или проблема с подключением

**Решение:**
1. Проверьте `PAYPAL_CLIENT_ID` и `PAYPAL_SECRET` в `config.py`
2. Убедитесь что используете правильный URL (sandbox или production)
3. Проверьте интернет соединение

### Бот не отвечает в Telegram

**Решение:**
1. Убедитесь что бот запущен (проверьте процесс)
2. Проверьте `BOT_TOKEN` в `config.py`
3. Посмотрите логи на наличие ошибок

### Ошибка при проверке карт

**Возможные причины:**
1. PayPal API временно недоступен - попробуйте позже
2. Превышен лимит запросов - подождите немного
3. Неправильный формат данных карты - используйте правильный формат
4. Проблемы с PayPal аккаунтом - проверьте Dashboard

## 📊 Мониторинг

### Просмотр активности бота:

```bash
# Если запущен через nohup
tail -f /workspace/bot.log

# Если запущен через systemd
sudo journalctl -u cardbot -f

# Если запущен через screen
screen -r cardbot
```

## 🔐 Безопасность

⚠️ **ВАЖНО:**

1. **Никогда не публикуйте** ваши PayPal credentials
2. **Не коммитьте** `config.py` с реальными credentials в git
3. **Ограничьте доступ** к боту (можете добавить проверку `ADMIN_IDS` в `config.py`)
4. **Используйте HTTPS** если разворачиваете на сервере
5. **Регулярно обновляйте** зависимости для безопасности

## 📈 Расширенные возможности

Можете расширить функционал бота:

1. **Добавить базу данных** - сохранять историю проверок
2. **Добавить лимиты** - ограничить количество проверок на пользователя
3. **Добавить платную подписку** - монетизация
4. **Добавить больше платежных систем** - Stripe, Square и др.
5. **Улучшить безопасность** - добавить капчу, 2FA

## 💡 Полезные команды

```bash
# Просмотр процессов Python
ps aux | grep python3

# Остановка всех процессов бота
pkill -f "bot.py"

# Проверка портов
netstat -tulpn | grep python

# Обновление зависимостей
pip3 install --upgrade -r requirements.txt
```

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи
2. Убедитесь что все credentials правильные
3. Проверьте интернет соединение
4. Попробуйте перезапустить бота

---

**Создано:** 2025-11-08  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию
